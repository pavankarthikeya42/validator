def parse_dom_payload(payload: dict) -> dict:
    """
    Parses the dynamic DOM payload.
    Since the payload is now structured as a flat key-value dictionary containing 
    the 32 target fields, we return it directly.
    """
    return payload

